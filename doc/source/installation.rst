============
Installation
============

At the command line::

    $ pip install networking-huawei

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-huawei
    $ pip install networking-huawei
